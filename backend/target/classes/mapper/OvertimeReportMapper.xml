<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinspace.mapper.OvertimeReportMapper">

    <!-- 根据用户ID获取加班记录列表 -->
    <select id="selectByUid" resultType="com.jinspace.entity.OvertimeReport">
        SELECT * FROM overtime_report WHERE uid = #{uid} ORDER BY report_date DESC
    </select>

    <!-- 根据用户ID和日期获取加班记录 -->
    <select id="selectByUidAndReportDate" resultType="com.jinspace.entity.OvertimeReport">
        SELECT * FROM overtime_report WHERE uid = #{uid} AND report_date = #{reportDate}
    </select>

    <!-- 根据用户ID和日期范围获取加班记录列表 -->
    <select id="selectByUidAndDateRange" resultType="com.jinspace.entity.OvertimeReport">
        SELECT * FROM overtime_report 
        WHERE uid = #{uid} 
        AND report_date &gt;= #{startDate} 
        AND report_date &lt;= #{endDate}
        ORDER BY report_date DESC
    </select>

</mapper>